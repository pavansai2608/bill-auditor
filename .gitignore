# Virtual environments
.venv/
venv/

# PyBuilder
.pybuilder/
target/
.coverage
htmlcov/

# Python-generated files
__pycache__/
*.py[oc]
*.pyc
build/
dist/
wheels/
*.egg-info
target/

# Secrets
.env

# Generated data (never commit)
data/db/
data/llm_cache/
data/retrieval_cache/
data/policies/*.pdf
data/traces/

# Eval run checkpoints - derived from the bills and the key, and machine-local.
# They exist so a crashed 44-bill run resumes instead of starting over.
eval/.cache/

# Eval run logs. Named eval_full.log, eval_full2.log and so on as runs are
# repeated, so the pattern rather than the names.
*.log

# Frontend
node_modules/

# Local tool settings, not part of the project
.claude/

# OS
.DS_Store
docker-compose.override.yml

# E2E server logs, written by tests/e2e/run_stage.sh
.e2e-logs/
frontend/dist/ba-build-stamp.txt

# Regenerated by tests/e2e/capture_screenshots.py. Only screen-0-* is
# kept, because frontend/design/landing/README.md cites it by name.
frontend/design/screenshots/screen-1-*.png
frontend/design/screenshots/screen-2-*.png
frontend/dist/
